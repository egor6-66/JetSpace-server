const UserDTO = require('./user-dto');
const MessageDTO = require('./messages-dto');
const LikeDTO = require('./like-dto');

const DTOs = {
    UserDTO,
    MessageDTO,
    LikeDTO
}

module.exports = DTOs